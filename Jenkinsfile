pipeline {
  agent any

  stages {  
    stage("Build and Deploy") {    
      steps {
      echo 'building & deploying the application'
      sh 'sudo cp WebService.js /home/centos'
      }      
    }
  }
}
